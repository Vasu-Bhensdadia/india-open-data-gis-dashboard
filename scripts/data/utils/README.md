# Shared GIS Utilities

This directory contains reusable helpers for common GIS processing tasks and metadata extraction.

Key responsibilities:

- Geometry validation and coordinate normalization.
- Metadata loading, schema alignment, and provenance extraction.
- Utility helpers shared across ingestion and optimization workflows.
