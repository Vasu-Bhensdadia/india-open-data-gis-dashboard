# GeoJSON Optimization

This directory contains tools for optimizing GeoJSON outputs and preparing lightweight vector assets.

Key responsibilities:

- Strip unnecessary properties from GeoJSON features.
- Normalize coordinate precision to reduce payload size.
- Simplify geometry with configurable tolerance for faster rendering.
- Prepare dataset variants for client delivery and analytics.
