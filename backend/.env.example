# Backend service configuration
NODE_ENV=development
PORT=4000

# Existing non-secret service defaults
HOST=0.0.0.0
LOG_LEVEL=info
API_PREFIX=/api
API_VERSION=v1

# Cross-origin access for the local frontend
CORS_ORIGINS=http://localhost:3000

# Dataset location from the backend workspace
DATASETS_DIR=../datasets
